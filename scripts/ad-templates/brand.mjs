// Shared design tokens — one source of truth for all rendered ad templates.
// Mirrors Devon's "one design system → all assets" principle.
// Client Ready palette: deep teal ink + amber accent on warm off-white.

export const brand = {
  name: "Client Ready",
  ink: "#12241F",        // near-black teal (headlines, body)
  inkSoft: "#5B6B66",    // muted labels
  accent: "#D9902B",     // amber (price, CTA, checkmarks)
  accentInk: "#12241F",  // text on amber
  bg: "#F7F5F0",         // warm off-white card
  line: "#E4E1D8",       // hairline separators
  imessageBlue: "#0A84FF",
  imessageGray: "#E9E9EB",
  fontUI: "-apple-system, BlinkMacSystemFont, 'SF Pro Text', 'Helvetica Neue', Arial, sans-serif",
  fontDisplay: "'SF Pro Display', -apple-system, 'Helvetica Neue', Arial, sans-serif",
};
